# .env.example — copy to .env on first checkout: cp .env.example .env
# All defaults work for local dev as-is. Add real secrets here when needed.

# PORT=3000       # override ingestion-api port
# SENTRY_DSN=     # future
